# 2026-08-11 Z-Library 手工 OCR 校对

用户要求：没有 API key，就本地一点一点读、一点一点改（"手工"）。

## 手工校对进度（OCR 书共 88 本，已做 4 本）
1. 清华出版社物理难题150例086890-01（4437字）—— 整本重写：按题号001-006分段、补句读、修错字（如 "（直径）是张力T"→"（直径 d）。设张力 T"；"以"0"标记原点"→"以 O 标记原点"）。
2. 电动力学南京大学ch0-0（8540字）—— 整本重写：删页眉垃圾(edu. nju. edu. cn/litao)、参考书列表去 `#`、删每章重复的"内容大纲"、修标题层级(章#/节##/小节###)、补 pp 图注乱码为 P/P'、整理格林定理等公式碎片。
3. 给孩子讲量子力学-李淼-1（30657字）—— 结构修复：目录整理、第1/2/3讲标题分行、ENIAC 参数碎片合并（17468根真空管…）、补"高约2.5米"。
4. 温州乡土建筑-丁俊清（16081字）—— 结构修复：# 2./3./第三章/五、/一、二、三、四、 标题分行（关键：标题前也要补换行，否则 TOC 正则 ^#{1,3} 识别不到）、错字 林徽音→林徽因、赘练→洗练、祺拜→崇拜、藏姓姓→姓氏。

## 脚本改进
- `scripts/ocr_local_fix.py` 的 `fix_cjk_spacing` 增加：数字-中文双向空格折叠（`1979 年`→`1979年`）、小数点后续数字空格（`20. 9`→`20.9`）。原只折叠 CJK 间空格，漏了数字年份空格。
- 加 `SKIP` 集合（4 本已手工的 slug），重跑其余 84 本时跳过，保留手工成果；`--slug` 指定时可强制重跑。

## 待续
- 重跑其余 84 本后应重建站点、提交。
- 继续手工：滕县商业志（有 "FORE"乱码、英文注入句 "The request was rejected..."、数字空格）等剩余 OCR 书。
- 推送：本地提交领先远程，需用户 `git push origin main` 输有效 PAT（wincred 旧 PAT 失效）。

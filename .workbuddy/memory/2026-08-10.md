# 2026-08-10 Z-Library 站点维护

## 状态核对
- GitHub Pages 已上线：`https://wadesha.github.io/zlibrary/`（根 + 书页均 200）
- git 跟踪 420 本（books/ 840 文件 = content.md+meta.json）+ site/books/ 420 页，工作树干净
- 本地 `ls books/ site/books/` 数字(426/440)是 WPS 云同步噪声，git 未跟踪，不影响线上
- 清理：删 1 个重复 books/ 目录(`2-`前缀) + 25 个失效 site 页；重跑 build_site.py 生成 420 页

## OCR 纠错试跑（hy3）
- 新建 `scripts/ocr_fix_test.py`：标准库 urllib 调 hy3，不依赖 requests/fitz；分块+限时，跑通流程
- **阻塞：hy3 API Key 失效** → 所有调用 401 `Invalid API Key`
  - key: `tp-c3m8y92r0nj7msaxho4lmxzw6i17ernmpqs8kkt0jn8cd1xs`（同样写在 ocr_pipeline.py）
  - 端点直连可达（无需代理）：`https://token-plan-cn.xiaomimimo.com/v1/chat/completions`
  - 待用户提供有效 key 后，更新 ocr_pipeline.py 与 ocr_fix_test.py 再全量跑 88 本 OCR 书

## 环境
- python=Python312(仅标准库)；git 代理 127.0.0.1:7897（仅 git 需要，hy3 端点直连）
- 项目根：`.../Z-Library/Z-Library-Web/`

## 本地 OCR 修复（无 API，agent 亲手做）
- 根因：OCR->Markdown 给几乎每行加 `## ` 前缀（整本书变巨标题）+ 中文每个字间插空格 + 断行碎片 + 个别错字。
- 新增 `scripts/ocr_local_fix.py`：fix_headings(去伪标题,只保留带结构标记的章标题) + fix_cjk_spacing(折叠CJK字间空格,保留英文词空格) + fix_chars(高精度错字规则) + fix_page_markers(`--- Page N ---`独立成行) + join_broken(合并碎片句,补空格)。
- 已对全部 88 本 OCR 书(ocr_md+hy3)跑通并重建站点(420页)。
- 手工校对样板 1 册：山西浑源永安寺壁画-金维诺（修 传人→传入、延祜→延祐、坂→呈、塔茹→塔身、画脱…恶882→画中有…怪八百余、张僧藩→张僧繇、补句读、删 ae/hh/aa/阮 等OCR垃圾）。
- 提交 d622884（本地，领先远程1）。**push 卡住：wincred 中 PAT 失效；需用户重输 PAT 推送**。

## 待续
- 其余 87 本 OCR 书可逐册手工校对（用户要"一点点做"）；先自动化再抽样手工。
- 推送命令：git push origin main（输入有效 PAT）。
